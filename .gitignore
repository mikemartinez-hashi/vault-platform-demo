# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Local variable / secret files
terraform.tfvars
*.auto.tfvars
.vault-outputs.env

# Local-only HCP Terraform backend (if you ever cp backend.tf.example -> backend.tf
# for a CLI-driven remote workspace). The GitHub Actions workflows no longer run
# Terraform, so this is not needed for the pipeline.
backend.tf

# OS cruft
.DS_Store
